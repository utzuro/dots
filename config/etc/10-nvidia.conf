# To check BusID: lspci | grep VGA
# Put it in /etc/X11/xorg.conf.d/10-nvidia.conf
# Needed in order for i3 to run
Section "Device"
    Identifier     "Device0"
    Driver         "nvidia"
    VendorName     "NVIDIA Corporation"
    BusID          "PCI:1:0:0"
EndSection
