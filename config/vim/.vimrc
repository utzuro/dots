so ~/.vim/basic.vim
so ~/.vim/maps.vim
so ~/.vim/auto.vim
so ~/.vim/plugins.vim
so ~/.vim/custom.vim
" so ~/.vim/light.vim

" colorscheme dracula

" disabel copilot by default
let g:copilot_enabled = v:false
